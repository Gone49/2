"use server";

import { RegisterInputProps } from "@/type/types";


export async function createUser(data: RegisterInputProps) {
    try {
       data)
      } catch (error) {
        console.log(error);
        
      }
    
}